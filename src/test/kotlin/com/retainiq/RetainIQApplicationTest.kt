package com.retainiq

import org.junit.jupiter.api.Test
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.test.context.ActiveProfiles

// @SpringBootTest  -- uncomment when full infra (DB, Redis) is available
class RetainIQApplicationTest {

    @Test
    fun contextPlaceholder() {
        // Placeholder — full context load test requires testcontainers
    }
}
