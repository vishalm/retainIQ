apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: retainiq
  namespace: retainiq
  labels:
    app: retainiq
spec:
  selector:
    matchLabels:
      app: retainiq
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
