apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: retainiq

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - ingress.yaml
  - servicemonitor.yaml
